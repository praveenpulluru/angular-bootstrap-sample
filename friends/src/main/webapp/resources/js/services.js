'use strict';

/* Services */

/*var nameService = angular.module('nameService', ['ngResource']);

searchServices.factory('nameService', [
		'$resource',
		'$location',
		'$route',
		'$rootScope',
		function($resource, $location, $route, $rootScope) {
			return $resource(
					'http://localhost:7001/home/name?:enterName', {}, {
						getPerson : {
							method : 'GET',
							params : {
								enterName : 'name'
							},
							isArray : false,
							headers : {'Accept' : 'application/json'}
						}
					});
		} ]);*/